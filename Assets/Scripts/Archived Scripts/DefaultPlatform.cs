using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefaultPlatform : MonoBehaviour
{


    //This script is only to be used for the default platforms shown at the beginning of the level.

    public float lowerScreenBound = -5f; // The Y position where the game ends
    public float fallSpeed = 2.5f;       // Speed of falling


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
